[project]
name = "agent-quality"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "itsorivera", email = "its.orivera@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "google-adk>=2.7.1",
    "langgraph>=1.2.11",
]

[project.scripts]
agent-quality = "agent_quality:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
